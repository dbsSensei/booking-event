import { bookings, bookEvent, cancelBooking } from './bookingController';
import { events, createEvent } from './eventController';
import { users, createUser, login } from './userController';

export default {
  bookings,
  bookEvent,
  cancelBooking,
  events,
  createEvent,
  users,
  createUser,
  login,
};
